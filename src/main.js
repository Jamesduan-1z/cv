import './style.css'
import { portfolio } from './content.js'

const { profile, projects, stats, skills, experience } = portfolio
let activeFilter = '全部项目'
const projectCard = (project, index) => `<article class="project-card reveal" style="--delay: ${index * 90}ms"><a class="project-image" href="#contact" aria-label="了解 ${project.title}"><img src="${project.image}" alt="${project.title} 项目视觉" loading="lazy"><span class="project-arrow" aria-hidden="true">↗</span></a><div class="project-meta"><span>${project.id} / ${project.year}</span><span>${project.category}</span></div><h3>${project.title}</h3><p>${project.description}</p><div class="tag-list">${project.tags.map((tag) => `<span>${tag}</span>`).join('')}</div></article>`
const renderAbout = () => { const { about } = portfolio; document.querySelector('#about').innerHTML = `<div class="section-heading"><div><p class="eyebrow">${about.eyebrow}</p><h2>${about.title}<br><em>${about.emphasis}</em></h2></div><span>${about.period}</span></div><div class="about-grid"><div class="about-lead"><p>${about.intro}</p><a class="text-link" href="mailto:${profile.email}">${about.introAction} <span>↗</span></a></div><div class="experience"><h3>${about.experienceTitle}</h3>${about.experience.map((item) => `<div class="experience-item"><span>${item.period}</span><div><strong>${item.company}</strong><b>${item.role}</b><small>${item.note}</small></div></div>`).join('')}</div><div class="skills"><h3>${about.skillsTitle}</h3><div class="skill-cloud">${about.skills.map((skill) => `<span>${skill}</span>`).join('')}</div></div></div>` }
const renderProjects = () => { const filtered = activeFilter === '全部项目' ? projects : projects.filter((project) => project.category === activeFilter); document.querySelector('#project-grid').innerHTML = filtered.map(projectCard).join(''); document.querySelector('#project-count').textContent = `${String(filtered.length).padStart(2, '0')} / ${String(projects.length).padStart(2, '0')}` }
document.querySelector('#app').innerHTML = `<header class="site-header"><a class="wordmark" href="#top" aria-label="返回顶部"><span>ZY</span> / portfolio</a><nav class="nav-links" aria-label="主导航"><a href="#work">作品 <sup>03</sup></a><a href="#about">关于</a><a href="#contact">联系</a></nav><button class="theme-toggle" id="theme-toggle" type="button" aria-label="切换显示模式"><span></span> 夜间模式</button></header><main id="top"><section class="hero section-wrap"><div class="hero-kicker"><span class="status-dot"></span> ${profile.availability}</div><div class="hero-copy"><p class="eyebrow">Independent designer · 06—24</p><h1>把复杂的事，<br><em>做得有感觉。</em></h1><p class="hero-intro">${profile.intro}</p></div><div class="hero-side"><span>Scroll to explore</span><span class="scroll-line"></span><span>↓</span></div><div class="hero-orbit" aria-hidden="true"><div class="orbit-ring"></div><div class="orbit-label">DESIGN<br>WITH<br>INTENT</div><div class="orbit-dot"></div></div><div class="hero-bottom"><span>Shanghai / CN</span><span class="hero-index">Selected work <strong>01—03</strong></span></div></section><section class="stats-strip section-wrap" aria-label="工作概览">${stats.map((stat) => `<div class="stat"><strong>${stat.value}</strong><span>${stat.label}</span></div>`).join('')}<p>不只做界面，<br>也在意它如何进入生活。</p></section><section class="work section-wrap" id="work"><div class="section-heading"><div><p class="eyebrow">01 / selected work</p><h2>最近在做的事</h2></div><span id="project-count">03 / 03</span></div><div class="filter-row" role="group" aria-label="筛选作品"><button class="filter active" data-filter="全部项目">全部项目</button><button class="filter" data-filter="产品体验">产品体验</button><button class="filter" data-filter="品牌视觉">品牌视觉</button><button class="filter" data-filter="数字产品">数字产品</button></div><div class="project-grid" id="project-grid"></div></section><section class="about section-wrap" id="about"><div class="section-heading"><div><p class="eyebrow">02 / a little about me</p><h2>让设计变成<br><em>一种关系。</em></h2></div><span>Profile / 2024</span></div><div class="about-grid"><div class="about-lead"><p>${profile.bio}</p><a class="text-link" href="mailto:${profile.email}">聊聊你的项目 <span>↗</span></a></div><div class="experience"><h3>经历</h3>${experience.map((item) => `<div class="experience-item"><span>${item.period}</span><div><strong>${item.company}</strong><b>${item.role}</b><small>${item.note}</small></div></div>`).join('')}</div><div class="skills"><h3>我擅长的</h3><div class="skill-cloud">${skills.map((skill) => `<span>${skill}</span>`).join('')}</div></div></div></section><section class="contact section-wrap" id="contact"><div class="contact-mark">↘</div><p class="eyebrow">03 / say hello</p><h2>有个想法？<br><em>一起把它做出来。</em></h2><a class="email-link" href="mailto:${profile.email}">${profile.email} <span>↗</span></a><div class="contact-footer"><span>© 2024 ${profile.name}</span><div>${profile.social.map((item) => `<a href="${item.href}" target="_blank" rel="noreferrer">${item.label}</a>`).join('')}</div><span>Built with curiosity</span></div></section></main>`
document.querySelector('h1 em').textContent = '做简单。'
document.querySelector('.wordmark span').textContent = 'DYX'
document.querySelector('#work .section-heading h2').textContent = '现在做的事'
renderAbout()
renderProjects()
document.querySelectorAll('.filter').forEach((button) => button.addEventListener('click', () => { activeFilter = button.dataset.filter; document.querySelectorAll('.filter').forEach((item) => item.classList.toggle('active', item === button)); renderProjects() }))
document.querySelector('#theme-toggle').addEventListener('click', () => { document.body.classList.toggle('light-mode'); document.querySelector('#theme-toggle').lastChild.textContent = document.body.classList.contains('light-mode') ? ' 日间模式' : ' 夜间模式' })
const observer = new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && entry.target.classList.add('visible')), { threshold: 0.12 }); document.querySelectorAll('.reveal').forEach((element) => observer.observe(element))
